# Documentation claim vs reality

Audit of documentation changes against code and OpenAPI runtime (working-tree diffs, `VALID_TRANSITIONS`, `app.openapi()`, and `TestClient`).

| Documentation claim | Code or runtime reality | Resolution | Evidence to keep |
| --- | --- | --- | --- |
| README §9: CORS allows `localhost` / `127.0.0.1` on ports `5500`, `5501`, `5173`, plus `null`. | `allow_origins` has `localhost:5173` but **not** `http://127.0.0.1:5173`. Origins are: `localhost:5500/5501/5173`, `127.0.0.1:5500/5501`, `null`. | Either list the exact six origins in the README, or add `http://127.0.0.1:5173` if that was intended. | `app/main.py` CORS list (lines 20–27). |
| README §9 / overview: status transitions are restricted to `ToDo→InProgress→Done→InProgress` (no skip / same-state), without saying PATCH-only. | Graph matches `VALID_TRANSITIONS` for **PATCH**. **POST** never calls `validate_status_transition`; `POST {"title":"x","status":"Done"}` returns **201** with `status: Done`. | Scope the claim to `PATCH /tasks/{id}` (and note create may set any `TaskStatus`). | `business_rules.VALID_TRANSITIONS`; `create_task` vs `update_task`; TestClient create-as-Done → 201. |
| Docstrings on GET/PATCH/DELETE: raise **404** when the task is missing. | Runtime does return 404. Generated OpenAPI/`/docs` responses are only **200/422** (GET/PATCH) and **204/422** (DELETE) — **404 is absent** from the schema. | Add explicit `responses={404: ...}` on those routes if `/docs` should match the docstrings, or note that raised `HTTPException`s are not auto-documented. | `app.openapi()` response keys; `raise HTTPException(status_code=404, ...)` in `main.py`. |
| README §9: `main.py` handlers have “no business logic of their own”. | Handlers call validators and implement due-date change gating (`due_date` in `model_fields_set` and value differs before validating). | Soften to: handlers orchestrate; rule implementations live in `business_rules.py`. | `update_task` in `app/main.py` (due-date / status checks before `storage.update_task`). |
| `storage.update_task` docstring `[VERIFY]`: explicit `null` clears optional fields (e.g. `assignee`). | Confirmed: create with `assignee: "bob"`, then `PATCH {"assignee": null}` → **200** and `assignee: null`. | Remove the `[VERIFY]` tag; keep the behavior note (it is accurate). | Docstring in `app/storage.py`; TestClient null-assignee PATCH. |
| Docstrings + tests imply **POST → 201**, **DELETE → 204**; OpenAPI schema names `TaskCreate` / `TaskUpdate` / `TaskResponse`. | Matches: `status_code=HTTP_201_CREATED` / `HTTP_204_NO_CONTENT`; OpenAPI responses `201`/`204`; request `$ref`s are those schema names; tests assert 201/204. | **Keep as-is** — no change needed for these claims. | `main.py` route decorators; `app.openapi()`; `tests/test_tasks.py` (`test_create_task_valid_returns_201_*`, `test_delete_existing_returns_204_no_body`). |
| README: Docker two-stage `python:3.11-slim`, non-root `app`, uvicorn without `--reload`; CI runs `pytest -v` on Python 3.11; `requirements.txt` is UTF-16LE with duplicate `pytest`/`httpx`. | Matches Dockerfile, `ci.yml`, and file bytes (`FF FE` BOM; `httpx`/`pytest` each listed twice). | **Keep as-is** (optional: normalize `requirements.txt` encoding later; not a doc error). | `Dockerfile`, `.github/workflows/ci.yml`, `requirements.txt` bytes / duplicate lines. |

**Highest-priority manual check if you paste `/docs`:** confirm whether 404 appears for GET/PATCH/DELETE — today the live OpenAPI schema will not show it even though the API returns it.
